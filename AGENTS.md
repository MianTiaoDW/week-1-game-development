# Codex 接管规则

按任务路由：大厅背景动画与大厅UI先读 `docs/HANDOFF_2026-09-04_LOBBY_ANIMATION_AND_UI_DIRECTION.md`，再按需读 `docs/HANDOFF_2026-08-31_LOBBY_UI.md`；角色选择、角色四视图、群像线稿分别读各自08-31交接。不得用一个任务的暂停/恢复状态覆盖另一个任务。大厅仍只做视觉探索；右上资源/设置不可擅改，六张规范中的示例不授权新增玩法，候选图不等于验收或可切透明资产。

0. **2026-09-04大厅任务更新**：当前先完成单镜头悬崖营地静态基准帧，再验证大厅UI并制作6–8秒轻待机循环。大透视、约8–10度轻荷兰角、白天无旺火、米露卡落地无扫帚、五人松散分布且朝向自然是已确认方向；最新候选的场景骨架可用，人物互动尚待修正。追逐群像转为开始冒险/加载/宣传素材候选并暂停补镜。五人群像线稿恢复时仍以 `docs/HANDOFF_2026-08-31_KEYART_AND_LINEART.md` 为事实源，不得混写任务状态。

1. `docs/HANDOFF_2026-08-28_RUNNER_AND_UI_DIRECTION.md` 负责玩法转向与早期UI历史；新视觉任务按上方路由读取，不沿用已被后续用户请求覆盖的执行顺序。按需阅读 `docs/HANDOFF_2026-08-27_CROSS_THREAD_SYNC.md` 与 `docs/HANDOFF_2026-08-27_CHARACTER_SELECT_UI.md` 追溯历史。
2. 角色四视图、表情图与道具图先读 `docs/HANDOFF_2026-08-31_CHARACTER_TURNAROUNDS.md`，再查看 `assets/character-masters/`；美宣、出场动画与较早角色背景先读 `docs/HANDOFF_2026-08-24.md`。`CHARACTER_VISUAL_HANDOFF_2026-08-21.md`、`CHARACTER_MJ_HANDOFF_2026-08-20.md` 和 `GAME_DESIGN_HANDOFF.md` 只用于追溯历史。
3. 五名角色的身份母版固定在 `assets/character-masters/`。图号必须按当次实际上传顺序解释；大厅最新编辑轮次为图1底图、图2至图6五名角色四视图，不能套用旧任务的图号。
4. 用户偏好短而有主次的提示词。先锁角色、故事瞬间、镜头骨架、主光和少量禁止项，不要用几十条平均权重的限制淹没模型。
5. 英雄联盟本地图库只用于分析构图、明暗、材质和空间语法，不复制角色、武器、服装、Logo 或一比一原画。
6. 连续三轮局部修补仍失败时，停止叠加编辑；回到角色母版和一张构图参考重新生成。
7. 未经用户明确确认，不得把候选美宣图标为定稿。文档中必须区分“已确认”“候选”“失败记录”和“尚未核验”。
8. 跑酷主玩法方向继续保留。2026-09-04 用户把当前工作切到大厅背景动画与大厅UI收敛；先按最新大厅交接完成静态基准帧、UI叠加验证和轻待机循环。角色选择UI、Logo、完整加载动画和波姆美宣保持各自原有未完成状态，不用旧优先级覆盖本次任务。
9. UI 资产由设计师绘制并切透明 PNG 是正式管线，不得把“方便开发”误解成所有组件必须退化为基础矢量。组件内部允许材质与微光，外围保持干净；动态文字与数值由程序叠加。
10. UI 当前方向为“温暖星路旅行”：奶油黄内容区、清浅蓝结构、暖橙主操作、少量薄荷绿。功能页可用完整旅行册面板，沉浸页只允许边缘小组件与无边界可读性遮罩；外部参考只学系统方法，不复制资产和商业功能。
11. 角色选择页的返回、双货币、角色信息圆图标和无底框左右箭头已锁定形状；只修改被用户点名的组件。角色卡宽高比为 3:4，常态卡、深蓝名字遮罩和黄色选中卡必须分层，禁止用普通圆角卡或黄描边白卡替代。
